简述Applet的生命周期及四个核心方法。**

生命周期：`init()` → `start()` → `stop()` → `destroy()`  
- `init()`：加载时调用，初始化，只执行一次。  
- `start()`：`init()`之后或每次返回页面时调用，启动执行，可多次。  
- `stop()`：离开页面时调用，暂停执行，可多次。  
- `destroy()`：浏览器关闭时调用，释放资源，只执行一次。